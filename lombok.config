# See: https://projectlombok.org/features/configuration

# Add @lombok.Generated annotations so JaCoCo can skip coverage on generated methods
lombok.addLombokGeneratedAnnotation = true

# Signal that this is the root directory
config.stopBubbling = true
